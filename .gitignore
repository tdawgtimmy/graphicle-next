# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage

# next.js
.next/
out/

# production
build

# nx
.nx/cache
.nx/workspace-data

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env*.example

# typescript
*.tsbuildinfo
next-env.d.ts

*storybook.log
storybook-static

# python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# intermediate openapi dump; the generated api.d.ts IS committed so the web app
# builds and typechecks without a Python toolchain (CI checks it is current)
apps/api/openapi.json

# scratchpad notes
notes/scratchpad